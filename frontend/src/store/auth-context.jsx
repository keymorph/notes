/*
    Store the authentitaction context for the user logged in so that it can be easily used throughout the app
*/

import React from "react";

const AuthContext = React.createContext();

export default AuthContext;
